#ifndef STRING_EXTEND_HPP
#define STRING_EXTEND_HPP 
#include <vector>
#include <string>
#include <sstream>

#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <map>

std::vector<std::string> split(const std::string& s, const std::vector<std::string>& t);
std::vector<std::string> split_lines(const std::string& s);
#endif